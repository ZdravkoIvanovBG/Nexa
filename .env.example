# Supabase project used for cloud library persistence (watchlist, currently
# watching, tier list). Copy to .env and fill in from the Supabase dashboard
# under Project Settings -> API. Both values are safe to ship in the client;
# Row Level Security is what protects user data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
